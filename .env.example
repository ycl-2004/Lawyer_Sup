# 前端环境变量示例 —— 复制为 .env.local 后按需修改（.env.local 已被忽略）。
# 不配置也能跑：默认同源 /api（单地址部署）或开发态回退到 http://localhost:8000。

# 后端 API 地址。单地址部署（后端托管前端）时留空即可；
# 前后端分离开发时指向后端，如 http://localhost:8000。
# VITE_API_BASE=http://localhost:8000
